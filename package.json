{
  "name": "pi-memory",
  "version": "1.2.0",
  "description": "Persistent project & user memory for pi — fast tech-stack detection, structured facts, agent-scoped memory, search/lint/compact tools, and budgeted prompt context",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
