{
  "name": "pi-memory",
  "version": "1.0.0",
  "description": "Persistent project & user memory for pi — auto-detects tech stack, learns conventions, and injects context so every session starts warm",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
